name: Daily Video Bot

on:
  schedule:
    # New York: 8:00 AM (Morning) | India: 6:30 PM (Evening)
    # GitHub UTC time use karta hai: 13:00 UTC
    - cron: '0 13 * * *' 
  workflow_dispatch:
  
permissions:
  contents: write         # Write permission zaroori hai delete/update ke liye

jobs:
  run-automation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: pip install requests

      - name: Run Main Script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
        run: python main.py

      - name: Commit & Push Changes
        run: |
          git config --global user.name "Auto Bot"
          git config --global user.email "bot@noreply.github.com"
          
          # Add changes (History update & Deleted videos)
          git add -A
          
          # Commit only if there are changes
          git diff --quiet && git diff --staged --quiet || (git commit -m "Automated: Processed & Deleted old videos" && git push)
